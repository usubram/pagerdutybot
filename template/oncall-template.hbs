{{#each result.teams}}
	{{summary}} \n
{{/each}}
{{#each result.escalation_policies}}
	{{summary}} \n
{{/each}}
{{#if result.users}}
	{{#if result.tooMany}}
		I found too many users on that lookup. I can also lookup by email or give it another try with full name. Below are top 3 users. \n
	{{/if}}
	{{#each result.users}}
		```
		Name: {{name}} \n
		{{#each contact_methods}}
			{{#if email}}
				Slack: <@{{idFromEmail address}}> {{presenceFromEmail address}} \n
			{{/if}}
			{{#if label}}
				{{label}}: {{#if country_code}} +{{country_code}}{{/if}} {{address}} \n
			{{/if}}
		{{/each}}
		{{#if job_title}}
			Title: {{job_title}} \n
		{{/if}}
		{{#if time_zone}}
			Time zone: {{time_zone}} \n
		{{/if}}
		{{#if teams}}
			Team: \n
			{{#each teams}}
				{{name}} \n
				{{html_url}} \n
			{{/each}}
		{{/if}}
		``` \n
	{{/each}}
{{/if}}
{{#each result.oncall}}
	{{#if summary}}
		*Escalation: {{name}}* \n
		{{#if oncall}}
			{{#each oncall}}
				{{#if level}}
				*Level {{level}}* \n
				{{/if}}
				```
				Name: {{name}} \n
				{{#each contact_methods}}
					{{#if email}}
						Slack: <@{{idFromEmail address}}> {{presenceFromEmail address}} \n
					{{/if}}
					{{#if label}}
						{{label}}: {{#if country_code}} +{{country_code}}{{/if}} {{address}} \n
					{{/if}}
				{{/each}}
				{{#if job_title}}
					Title: {{job_title}} \n
				{{/if}}
				``` \n
			{{/each}}
		{{else}}
			```
			None assigned at this level.
			``` \n
		{{/if}}
	{{/if}}
{{/each}}
{{#if result.user_not_found}}
	I did not find anyone by that name. I can also lookup by email or give it another try with full name.
{{/if}}
{{#if result.not_found}}
	I did not find anything by that name.
{{/if}}
{{#if result.too_many}}
	Too many results.
{{/if}}
{{#if result.no_users}}
	On call not assigned at this level.
{{/if}}
{{#if result.min_len_err}}
	I need minimum of 3 characters to help you.
{{/if}}
